# Frontend — variables d'environnement (aucune valeur réelle, voir plan.md § 3.8)

# Origine du backend FastAPI, utilisée uniquement côté serveur par les rewrites
# de next.config.ts (proxy same-origin -> cookie httpOnly utilisable, zéro CORS).
# Dev local : http://localhost:8000
BACKEND_ORIGIN=
